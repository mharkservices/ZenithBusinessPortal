{
  "name": "zenith-business-portal-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "dotenv -e src/config/.env -- nodemon server.js",
    "start": "dotenv -e src/config/.env -- node server.js",
    "db:migrate": "dotenv -e src/config/.env -- prisma migrate dev",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^6.9.0",
    "axios": "^1.10.0",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.0",
    "dotenv-cli": "^8.0.0",
    "express": "^4.18.0",
    "express-validator": "^7.0.0",
    "jsonwebtoken": "^9.0.0",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.0",
    "prisma": "^6.9.0"
  }
}
